# 试验详情图表模块 - 测试用例文档

**版本**: v1.0  
**更新日期**: 2026-06-13  
**测试范围**: 试验详情页顶部图表区（3个图表 + 4张数字卡片）

---

## 一、测试环境说明

### 1.1 前置条件
- MySQL 服务运行中，数据库 `clinical_trial_db` 已初始化
- 执行 `npm run db:init` 完成 Schema 推送和种子数据填充
- 使用 ADMIN 账号登录：`admin@ctms.com` / 密码 `password`

### 1.2 数据构造建议
建议在测试前准备以下三个试验项目，用于覆盖不同边界场景：

| 试验编号 | 用途 | 数据状态 |
|---------|------|---------|
| T-FULL-001 | 完整数据测试 | 5种受试者状态×5种访视状态×5种AE严重程度都有数据 |
| T-EMPTY-002 | 空数据测试 | 无任何受试者、访视、AE记录（全新创建的试验） |
| T-PARTIAL-003 | 部分类别为0 | 只有部分枚举状态有数据（如只有SCREENING和ENROLLED） |

---

## 二、测试用例总览

| 模块 | 用例数 | 优先级 | 覆盖范围 |
|------|-------|--------|---------|
| 1. 受试者状态分布环形图 | 8 | P0-P1 | 0值显示/全类别/空状态/颜色/图例/Tooltip |
| 2. 访视完成情况柱状图 | 7 | P0-P1 | 0值显示/全类别/空状态/颜色/Tooltip |
| 3. AE严重程度分布柱状图 | 7 | P0-P1 | 0值显示/全类别/空状态/颜色/Tooltip |
| 4. API数据层正确性 | 5 | P0 | distribution完整性/枚举键/数据准确性 |
| 5. 集成与回归 | 4 | P1 | 整体布局/响应式/性能 |

---

## 三、详细测试用例

### 模块 1：受试者状态分布（环形图）

#### TC-SUB-001 【P0】 5种状态全部有数据时完整显示
| 项目 | 说明 |
|------|------|
| 前提 | 试验T-FULL-001下 5 种受试者状态（SCREENING/ENROLLED/ACTIVE/WITHDRAWN/COMPLETED）各有 ≥1 条记录 |
| 步骤 | 1. 进入 /trials/{id} 详情页<br>2. 观察第一个图表 |
| 预期结果 | 1. 环形图显示5个扇形块<br>2. 图例在右侧垂直排列，共5项：筛选中/已入组/在研中/退出/完成<br>3. 图例每项后括号内显示对应数字（与实际数据一致）<br>4. 扇形颜色顺序：橙→蓝→绿→红→紫 |

#### TC-SUB-002 【P0】 部分类别为0时仍显示在图例中
| 项目 | 说明 |
|------|------|
| 前提 | 试验T-PARTIAL-003下 只有 SCREENING=3、ENROLLED=5，其他3种状态均为0 |
| 步骤 | 进入试验详情页，观察环形图 |
| 预期结果 | 1. 环形图只绘制2个扇形（SCREENING+ENROLLED，WITHDRAWN等0值不占扇区面积）<br>2. 图例仍显示完整5项：筛选中(3)、已入组(5)、在研中(0)、退出(0)、完成(0)<br>3. 0值项在图例中正常显示，灰色对应 |

#### TC-SUB-003 【P0】 所有类别都为0时显示"暂无数据"
| 项目 | 说明 |
|------|------|
| 前提 | 试验T-EMPTY-002 下无任何受试者记录（实际 subjectCount=0） |
| 步骤 | 进入试验详情页 |
| 预期结果 | 1. 不显示环形图<br>2. 图表区域中央显示灰色文字："暂无数据"<br>3. 卡片标题"受试者状态分布"正常显示 |

#### TC-SUB-004 【P1】 鼠标悬停扇形显示数字 Tooltip
| 项目 | 说明 |
|------|------|
| 前提 | 试验存在 ≥1 种状态受试者 |
| 步骤 | 鼠标移动到环形图任一扇形块上 |
| 预期结果 | 弹出白色浮层，内容：<br>- 第一行：状态名称（加粗，如"在研中"）<br>- 第二行：数量: N（加粗数字） |

#### TC-SUB-005 【P1】 每种类别颜色与设计一致
| 项目 | 说明 |
|------|------|
| 步骤 | 对照颜色清单检查 |
| 预期结果 | 1. SCREENING 筛选中 = 橙 `#F59E0B`<br>2. ENROLLED 已入组 = 蓝 `#3B82F6`<br>3. ACTIVE 在研中 = 绿 `#22C55E`<br>4. WITHDRAWN 退出 = 红 `#EF4444`<br>5. COMPLETED 完成 = 紫 `#8B5CF6` |

#### TC-SUB-006 【P1】 受试者总数