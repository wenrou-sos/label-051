# 试验锁库功能测试用例

## 一、功能概述
试验结束后需要锁库保证数据完整性。锁库后该试验下的受试者、访视、不良事件全部变为只读（API 层面拒绝 PUT/POST/DELETE 操作，前端按钮置灰禁用）。只有 ADMIN 可以解锁。锁库/解锁操作写入审计日志，记录操作人、操作时间和原因。

## 二、测试环境
- 测试环境：开发环境
- 测试账号：ADMIN 角色账号、普通用户角色账号
- 测试数据：至少 1 个处于非锁库状态的试验，包含受试者、访视、不良事件数据

## 三、测试用例

### 用例 1：数据库 Schema 验证（LOCKED 状态）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-001 |
| 用例名称 | TrialStatus 枚举包含 LOCKED 值 |
| 优先级 | 高 |
| 前置条件 | Prisma Schema 已更新，数据库已迁移 |

**测试步骤：**
1. 查看 Prisma Schema 中 TrialStatus 枚举定义
2. 确认枚举值包含 LOCKED

**预期结果：**
- TrialStatus 枚举中存在 LOCKED 值
- 数据库中 trial_status 类型支持 LOCKED 值

---

### 用例 2：锁库接口权限验证（非 ADMIN 用户）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-002 |
| 用例名称 | 非 ADMIN 用户无法锁库 |
| 优先级 | 高 |
| 前置条件 | 存在一个非锁库状态的试验，当前用户非 ADMIN 角色 |

**测试步骤：**
1. 使用非 ADMIN 用户登录
2. 调用 POST /api/trials/{id}/lock 接口

**预期结果：**
- 接口返回 403 状态码
- 返回错误信息：无权限执行锁库操作
- 试验状态保持不变

---

### 用例 3：锁库接口功能验证（ADMIN 用户）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-003 |
| 用例名称 | ADMIN 用户可以锁库 |
| 优先级 | 高 |
| 前置条件 | 存在一个非锁库状态的试验，当前用户为 ADMIN 角色 |

**测试步骤：**
1. 使用 ADMIN 用户登录
2. 调用 POST /api/trials/{id}/lock 接口，传入 reason 参数
3. 检查试验状态
4. 检查审计日志

**预期结果：**
- 接口返回 200 状态码
- 试验状态变为 LOCKED
- 审计日志中存在 LOCK 类型记录，包含操作人、操作时间
- 审计日志中存在 LOCK_REASON 类型记录，包含锁库原因

---

### 用例 4：重复锁库验证

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-004 |
| 用例名称 | 已锁库试验无法再次锁库 |
| 优先级 | 中 |
| 前置条件 | 存在一个已锁库状态（LOCKED）的试验，当前用户为 ADMIN 角色 |

**测试步骤：**
1. 使用 ADMIN 用户登录
2. 调用 POST /api/trials/{id}/lock 接口

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已处于锁库状态
- 试验状态保持 LOCKED 不变

---

### 用例 5：解锁接口权限验证（非 ADMIN 用户）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-005 |
| 用例名称 | 非 ADMIN 用户无法解锁 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，当前用户非 ADMIN 角色 |

**测试步骤：**
1. 使用非 ADMIN 用户登录
2. 调用 POST /api/trials/{id}/unlock 接口

**预期结果：**
- 接口返回 403 状态码
- 返回错误信息：无权限执行解锁操作
- 试验状态保持 LOCKED 不变

---

### 用例 6：解锁接口功能验证（ADMIN 用户）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-006 |
| 用例名称 | ADMIN 用户可以解锁 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，当前用户为 ADMIN 角色 |

**测试步骤：**
1. 使用 ADMIN 用户登录
2. 调用 POST /api/trials/{id}/unlock 接口，传入 targetStatus 和 reason 参数
3. 检查试验状态
4. 检查审计日志

**预期结果：**
- 接口返回 200 状态码
- 试验状态变为 targetStatus 指定的状态（默认 COMPLETED）
- 审计日志中存在 UNLOCK 类型记录，包含操作人、操作时间
- 审计日志中存在 UNLOCK_REASON 类型记录，包含解锁原因

---

### 用例 7：受试者创建 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-007 |
| 用例名称 | 已锁库试验无法创建受试者 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 调用 POST /api/subjects 接口，指定 trialId 为已锁库的试验ID
2. 传入有效的受试者数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法添加受试者
- 受试者未被创建

---

### 用例 8：受试者更新 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-008 |
| 用例名称 | 已锁库试验下的受试者无法更新 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有受试者 |

**测试步骤：**
1. 调用 PUT /api/subjects/{id} 接口，指定已锁库试验下的受试者ID
2. 传入更新数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法修改受试者
- 受试者数据保持不变

---

### 用例 9：受试者删除 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-009 |
| 用例名称 | 已锁库试验下的受试者无法删除 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有受试者 |

**测试步骤：**
1. 调用 DELETE /api/subjects/{id} 接口，指定已锁库试验下的受试者ID

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法删除受试者
- 受试者数据保持不变

---

### 用例 10：访视创建 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-010 |
| 用例名称 | 已锁库试验无法创建访视 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 调用 POST /api/visits 接口，指定 trialId 为已锁库的试验ID
2. 传入有效的访视数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法添加访视
- 访视未被创建

---

### 用例 11：访视更新 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-011 |
| 用例名称 | 已锁库试验下的访视无法更新 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有访视 |

**测试步骤：**
1. 调用 PUT /api/visits/{id} 接口，指定已锁库试验下的访视ID
2. 传入更新数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法修改访视
- 访视数据保持不变

---

### 用例 12：访视删除 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-012 |
| 用例名称 | 已锁库试验下的访视无法删除 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有访视 |

**测试步骤：**
1. 调用 DELETE /api/visits/{id} 接口，指定已锁库试验下的访视ID

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法删除访视
- 访视数据保持不变

---

### 用例 13：不良事件创建 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-013 |
| 用例名称 | 已锁库试验无法创建不良事件 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 调用 POST /api/adverse-events 接口，指定 trialId 为已锁库的试验ID
2. 传入有效的不良事件数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法添加不良事件
- 不良事件未被创建

---

### 用例 14：不良事件更新 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-014 |
| 用例名称 | 已锁库试验下的不良事件无法更新 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有不良事件 |

**测试步骤：**
1. 调用 PUT /api/adverse-events/{id} 接口，指定已锁库试验下的不良事件ID
2. 传入更新数据

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法修改不良事件
- 不良事件数据保持不变

---

### 用例 15：不良事件删除 - 锁库校验

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-015 |
| 用例名称 | 已锁库试验下的不良事件无法删除 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有不良事件 |

**测试步骤：**
1. 调用 DELETE /api/adverse-events/{id} 接口，指定已锁库试验下的不良事件ID

**预期结果：**
- 接口返回 400 状态码
- 返回错误信息：试验已锁库，无法删除不良事件
- 不良事件数据保持不变

---

### 用例 16：试验详情页 - 锁库按钮显示

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-016 |
| 用例名称 | 试验详情页锁库/解锁按钮显示验证 |
| 优先级 | 高 |
| 前置条件 | 存在一个非锁库状态的试验 |

**测试步骤：**
1. 使用 ADMIN 用户登录，进入试验详情页
2. 检查页面是否显示"锁库"按钮
3. 点击锁库按钮，输入原因，确认锁库
4. 检查试验状态是否变为"已锁库"
5. 检查页面是否显示"解锁"按钮
6. 使用非 ADMIN 用户登录，进入试验详情页
7. 检查是否显示锁库/解锁按钮

**预期结果：**
- ADMIN 用户在非锁库状态下看到"锁库"按钮
- 锁库成功后，状态标签变为"已锁库"（深灰色背景）
- 锁库成功后，显示"解锁"按钮
- 非 ADMIN 用户不显示锁库/解锁按钮

---

### 用例 17：试验详情页 - 编辑按钮禁用

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-017 |
| 用例名称 | 锁库状态下试验编辑按钮禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 进入已锁库试验的详情页
2. 检查编辑按钮的状态

**预期结果：**
- 编辑按钮置灰禁用
- 显示"已锁库，无法编辑"提示文字
- 点击按钮无反应

---

### 用例 18：试验编辑页 - 锁库提示

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-018 |
| 用例名称 | 锁库状态下试验编辑页显示提示并禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 进入已锁库试验的编辑页
2. 检查页面是否显示锁库提示横幅
3. 检查所有输入框是否可编辑
4. 检查保存按钮是否可用

**预期结果：**
- 页面顶部显示黄色警告提示横幅
- 提示文字：该试验已锁库，无法编辑。如需修改，请先由管理员解锁试验。
- 所有输入框、下拉框均禁用（或只读）
- 保存按钮置灰禁用，显示"已锁库，无法保存"

---

### 用例 19：受试者详情页 - 编辑/删除按钮禁用

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-019 |
| 用例名称 | 锁库状态下受试者详情页按钮禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有受试者 |

**测试步骤：**
1. 进入已锁库试验下的受试者详情页
2. 检查编辑按钮的状态
3. 检查删除按钮的状态

**预期结果：**
- 编辑按钮置灰禁用，显示"已锁库，无法编辑"
- 删除按钮置灰禁用，显示"已锁库，无法删除"
- 按钮无点击效果

---

### 用例 20：受试者编辑页 - 锁库提示

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-020 |
| 用例名称 | 锁库状态下受试者编辑页显示提示并禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有受试者 |

**测试步骤：**
1. 进入已锁库试验下受试者的编辑页
2. 检查页面是否显示锁库提示横幅
3. 检查保存按钮是否可用

**预期结果：**
- 页面显示黄色警告提示横幅
- 提示文字：该试验已锁库，无法编辑受试者。
- 保存按钮置灰禁用，显示"已锁库，无法保存"

---

### 用例 21：访视详情页 - 编辑按钮禁用

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-021 |
| 用例名称 | 锁库状态下访视详情页编辑按钮禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有访视 |

**测试步骤：**
1. 进入已锁库试验下的访视详情页
2. 检查编辑按钮的状态

**预期结果：**
- 编辑按钮置灰禁用，显示"已锁库，无法编辑"
- 按钮无点击效果

---

### 用例 22：访视编辑页 - 锁库提示

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-022 |
| 用例名称 | 锁库状态下访视编辑页显示提示并禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有访视 |

**测试步骤：**
1. 进入已锁库试验下访视的编辑页
2. 检查页面是否显示锁库提示横幅
3. 检查保存按钮是否可用

**预期结果：**
- 页面显示黄色警告提示横幅
- 提示文字：该试验已锁库，无法编辑访视。
- 保存按钮置灰禁用，显示"已锁库，无法保存"

---

### 用例 23：不良事件详情页 - 编辑按钮禁用

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-023 |
| 用例名称 | 锁库状态下不良事件详情页编辑按钮禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有不良事件 |

**测试步骤：**
1. 进入已锁库试验下的不良事件详情页
2. 检查编辑按钮的状态

**预期结果：**
- 编辑按钮置灰禁用，显示"已锁库，无法编辑"
- 按钮无点击效果

---

### 用例 24：不良事件编辑页 - 锁库提示

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-024 |
| 用例名称 | 锁库状态下不良事件编辑页显示提示并禁用 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，且该试验下有不良事件 |

**测试步骤：**
1. 进入已锁库试验下不良事件的编辑页
2. 检查页面是否显示锁库提示横幅
3. 检查保存按钮是否可用

**预期结果：**
- 页面显示黄色警告提示横幅
- 提示文字：该试验已锁库，无法编辑不良事件。
- 保存按钮置灰禁用，显示"已锁库，无法保存"

---

### 用例 25：审计日志验证 - 锁库

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-025 |
| 用例名称 | 锁库操作审计日志记录验证 |
| 优先级 | 高 |
| 前置条件 | 存在一个非锁库状态的试验，当前用户为 ADMIN 角色 |

**测试步骤：**
1. 记录当前时间
2. 对试验执行锁库操作，输入锁库原因
3. 查询该试验的审计日志列表
4. 检查锁库相关的日志记录

**预期结果：**
- 存在 LOCK 类型的审计日志
- LOCK 日志包含：操作人ID、操作人姓名、操作时间、操作类型为 LOCK
- 存在 LOCK_REASON 类型的审计日志
- LOCK_REASON 日志包含：锁库原因详情
- 操作时间与执行时间一致

---

### 用例 26：审计日志验证 - 解锁

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-026 |
| 用例名称 | 解锁操作审计日志记录验证 |
| 优先级 | 高 |
| 前置条件 | 存在一个已锁库状态的试验，当前用户为 ADMIN 角色 |

**测试步骤：**
1. 记录当前时间
2. 对试验执行解锁操作，输入解锁原因
3. 查询该试验的审计日志列表
4. 检查解锁相关的日志记录

**预期结果：**
- 存在 UNLOCK 类型的审计日志
- UNLOCK 日志包含：操作人ID、操作人姓名、操作时间、操作类型为 UNLOCK
- 存在 UNLOCK_REASON 类型的审计日志
- UNLOCK_REASON 日志包含：解锁原因详情
- 操作时间与执行时间一致

---

### 用例 27：状态标签验证

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-LOCK-027 |
| 用例名称 | LOCKED 状态标签和颜色验证 |
| 优先级 | 中 |
| 前置条件 | 存在一个已锁库状态的试验 |

**测试步骤：**
1. 进入试验列表页
2. 查看已锁库试验的状态标签
3. 进入试验详情页
4. 查看状态标签

**预期结果：**
- 状态标签文字为"已锁库"
- 状态标签颜色为深灰色背景（bg-slate-700 text-white）
- 列表页和详情页显示一致

---

## 四、回归测试

### RT-01：非锁库状态功能不受影响

**验证内容：**
- 未锁库的试验可以正常创建、编辑、删除受试者
- 未锁库的试验可以正常创建、编辑、删除访视
- 未锁库的试验可以正常创建、编辑、删除不良事件
- 试验状态可以正常切换（DRAFT → RECRUITING → ACTIVE → COMPLETED）

**预期结果：**
- 所有非锁库状态下的功能正常运行
- 不受 LOCKED 状态的影响

---

### RT-02：权限系统正常

**验证内容：**
- ADMIN 角色拥有 trial:lock 和 trial:unlock 权限
- 其他角色（如 CRA、MONITOR、SPONSOR）不拥有这两个权限
- 其他原有权限不受影响

**预期结果：**
- 权限配置正确
- 只有 ADMIN 可以执行锁库/解锁操作

---

## 五、测试执行记录

| 用例编号 | 测试结果 | 测试人 | 测试日期 | 备注 |
|---------|---------|-------|---------|------|
| TC-LOCK-001 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-002 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-003 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-004 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-005 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-006 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-007 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-008 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-009 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-010 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-011 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-012 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-013 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-014 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-015 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-016 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-017 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-018 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-019 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-020 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-021 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-022 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-023 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-024 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-025 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-026 | ☐ 通过 / ☐ 失败 | | | |
| TC-LOCK-07 | ☐ 通过 / ☐ 失败 | | | |
| RT-01 | ☐ 通过 / ☐ 失败 | | | |
| RT-02 | ☐ 通过 / ☐ 失败 | | | |

---

## 六、风险与注意事项

1. **数据库迁移**：部署前需执行 Prisma migrate，确保 TrialStatus 枚举包含 LOCKED 值
2. **数据备份**：生产环境锁库操作前建议进行数据备份
3. **权限控制**：确保只有 ADMIN 角色有 trial:lock 和 trial:unlock 权限
4. **审计日志**：锁库/解锁操作必须记录审计日志，便于追溯
5. **用户体验**：锁库状态下的按钮禁用和提示信息需要清晰明了
6. **API 一致性**：所有相关 API 都需要进行锁库校验，确保数据安全
