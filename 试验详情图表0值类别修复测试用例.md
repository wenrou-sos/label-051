# 试验详情图表0值类别修复测试用例

## 测试目标

验证试验详情页三张统计图在部分类别数量为0时，仍按业务枚举完整展示所有类别；只有对应业务总数为0时，才显示暂无数据。

## 涉及范围

涉及页面为试验详情页`/trials/{id}`，涉及接口为`GET /api/trials/{id}`，涉及图表为受试者状态分布、访视完成情况、AE严重程度分布。

## 修复说明

- **问题**：柱状图（访视完成情况、AE严重程度分布）的0值类别柱子高度为0，完全不可见
- **修复**：将柱状图 `Bar` 组件的 `minPointSize` 从 `0` 改为 `3`，确保0值类别仍有最小高度可见
- **文件**：`src/app/trials/[id]/charts-client.tsx`

---

## 用例1：接口返回完整受试者状态分布

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-API-001 |
| 优先级 | P0 |
| 前置条件 | 准备一个至少有1名受试者的试验，且受试者状态没有覆盖筛选中、已入组、在研中、退出、完成全部5类 |

**测试步骤**：
1. 使用管理员账号登录系统。
2. 打开该试验详情页。
3. 查看`GET /api/trials/{id}`接口返回的`data.statistics.subjectStatusDistribution`。

**预期结果**：
- 接口返回SCREENING、ENROLLED、ACTIVE、WITHDRAWN、COMPLETED全部5个键
- 没有记录的状态值为0，不返回null、undefined或缺失键

---

## 用例2：受试者状态图例展示0值类别

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-SUB-001 |
| 优先级 | P0 |
| 前置条件 | 同用例1，试验有部分状态的受试者，部分状态为0 |

**测试步骤**：
1. 打开该试验详情页。
2. 查看受试者状态分布图。

**预期结果**：
- 右侧图例展示筛选中、已入组、在研中、退出、完成全部5项
- 数量为0的状态仍显示在图例中并标记为(0)
- 只有非0状态绘制环形图扇区，0值状态不占扇区面积
- 图例中0值项的颜色指示点仍正常显示对应颜色

---

## 用例3：接口返回完整访视状态分布

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-API-002 |
| 优先级 | P0 |
| 前置条件 | 准备一个至少有1条访视记录的试验，且访视状态没有覆盖待访视、进行中、已完成、未访视、已取消全部5类 |

**测试步骤**：
1. 使用管理员账号登录系统。
2. 打开该试验详情页。
3. 查看`GET /api/trials/{id}`接口返回的`data.statistics.visitStatusDistribution`。

**预期结果**：
- 接口返回SCHEDULED、IN_PROGRESS、COMPLETED、MISSED、CANCELLED全部5个键
- 没有记录的状态值为0

---

## 用例4：访视完成情况柱状图展示0值类别

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-VISIT-001 |
| 优先级 | P0 |
| 前置条件 | 同用例3，试验有部分状态的访视，部分状态为0 |

**测试步骤**：
1. 打开该试验详情页。
2. 查看访视完成情况柱状图。
3. 鼠标悬停在0值类别的柱子或对应位置。

**预期结果**：
- X轴展示待访视、进行中、已完成、未访视、已取消全部5类
- 0值类别仍保留柱子位置，并以最小高度（3px）显示（可见一条细横线）
- 悬停提示中的数量为0
- 0值柱子的颜色与对应状态的配色一致

---

## 用例5：接口返回完整AE严重程度分布

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-API-003 |
| 优先级 | P0 |
| 前置条件 | 准备一个至少有1条AE记录的试验，且AE严重程度没有覆盖轻度、中度、重度、危及生命、死亡全部5类 |

**测试步骤**：
1. 使用管理员账号登录系统。
2. 打开该试验详情页。
3. 查看`GET /api/trials/{id}`接口返回的`data.statistics.aeSeverityDistribution`。

**预期结果**：
- 接口返回MILD、MODERATE、SEVERE、LIFE_THREATENING、FATAL全部5个键
- 没有记录的严重程度值为0

---

## 用例6：AE严重程度柱状图展示0值类别

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-AE-001 |
| 优先级 | P0 |
| 前置条件 | 同用例5，试验有部分严重程度的AE，部分严重程度为0 |

**测试步骤**：
1. 打开该试验详情页。
2. 查看AE严重程度分布柱状图。
3. 鼠标悬停在0值类别的柱子或对应位置。

**预期结果**：
- X轴展示轻度、中度、重度、危及生命、死亡全部5类
- 0值类别仍保留柱子位置，并以最小高度（3px）显示（可见一条细横线）
- 悬停提示中的数量为0
- 0值柱子的颜色与对应严重程度的配色一致

---

## 用例7：全空数据时显示暂无数据

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-EMPTY-001 |
| 优先级 | P0 |
| 前置条件 | 准备一个没有受试者、访视和AE记录的空试验 |

**测试步骤**：
1. 打开该试验详情页。
2. 查看三张图表卡片。

**预期结果**：
- 受试者状态分布、访视完成情况、AE严重程度分布三张卡片均显示"暂无数据"
- 页面不显示空白图表，也不误显示只有0值的图表
- "暂无数据"文字居中显示，颜色为灰色

---

## 用例8：混合边界场景验证

| 项目 | 说明 |
|------|------|
| 用例编号 | TC-MIX-001 |
| 优先级 | P1 |
| 前置条件 | 准备一个试验，其中：受试者有数据、访视有数据、AE完全没有数据 |

**测试步骤**：
1. 打开该试验详情页。
2. 依次查看三张图表。

**预期结果**：
- 受试者状态分布图正常显示，包含所有5类（0值类别在图例中显示）
- 访视完成情况柱状图正常显示，包含所有5类（0值类别有最小高度柱子）
- AE严重程度分布图显示"暂无数据"
- 三张图表卡片高度一致，布局整齐

---

## 回归检查点

| 检查点 | 预期结果 |
|--------|----------|
| 顶部数字卡片 | 受试者总数、访视总数、不良事件数仍显示正确数值 |
| 图表区布局 | 图表区仍位于数字卡片下方，三列布局（大屏）/单列布局（小屏） |
| 页面其他模块 | 图表下方的基本信息、组织信息、备注和受试者列表不受影响 |
| 响应式布局 | 在不同屏幕尺寸下图表自适应，不出现横向滚动条 |
| Tooltip 显示 | 鼠标悬停非0值柱子/扇区时，Tooltip正常显示名称和数量 |
| 颜色一致性 | 各状态的颜色与设计规范一致，0值类别颜色与对应状态一致 |
